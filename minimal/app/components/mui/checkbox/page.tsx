// sections
import CheckboxView from "minimal/sections/_examples/mui/checkbox-view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "MUI: Checkbox",
};

export default function CheckboxPage() {
  return <CheckboxView />;
}
