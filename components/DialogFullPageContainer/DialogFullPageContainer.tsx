import { styled } from "@mui/material/styles";

const DialogFullPageContainer = styled("div")(({ theme }) => ({
  padding: theme.spacing(2, 3),
  overflowY: "auto",
}));

export default DialogFullPageContainer;
