import { styled } from "@mui/material/styles";

const DialogFullPageHeader = styled("div")(({ theme }) => ({
  display: "flex",
  margin: theme.spacing(2, 0),
  padding: theme.spacing(0, 2),
}));

export default DialogFullPageHeader;
